# Method Authoring

This guide will describe the method-author workflow for NeuroMouse plugins.

For now, method authors should treat `DATA_CONTRACT.md` as the data-shape source of truth and
expect runnable examples to land under `methods/`.
