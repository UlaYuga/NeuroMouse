source-data/
node_modules/
test-results/
bench/perf-results.json
bench/perf-report.md
.env
.env.*
.DS_Store
__pycache__/
*.pyc

# MkDocs build output — committed on purpose: the static server serves it at /docs/
# (rebuild with `uv run mkdocs build` after editing docs/).

# backend runtime DB
*.sqlite3

# local design-reference material for the Cloud Design app-restyle (screenshots + spec);
# read locally by Cloud Design, not part of the deployed product
design-reference/
