# Contributing

NeuroMouse is moving from a zero-build browser viewer to a monorepo platform.

For now:

- Keep the existing root app stable.
- Write tests before implementation.
- Run the relevant uv and Node verification commands before review.
- Never edit files in `datasets/golden` directly.
